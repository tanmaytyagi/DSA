1. Big O Notation :
-------------------

Big O is a notation used to describe the "computational complexity" of an algorithm.

Time Complexity : it is the amount of time the algorithm needs to run relative to the input size.
Space Complexity : it is the amount of memory used by the algorithm relative to the input size.

Big O Sheet : https://www.bigocheatsheet.com/


--------------------------------------------------------------------------------------------

2. Subarrays/substrings, subsequences, and subsets :
----------------------------------------------------

a. Subarrays/substrings : it is a contiguous section of an array or string.
b. Subsequence : it is a set of elements of an array/string that keeps the same relative order but doesn't need to be contiguous.
c. Subsets : it is any set of elements from the original array or string (order doesn't matter).

--------------------------------------------------------------------------------------------

3. top down and bottom up approach in Dynamic programming :

top down : recursive (start from bigger problem and break into smaller pieces)
bottom up : iterative (start from smaller and build up to bigger required problem)

--------------------------------------------------------------------------------------------

Tricky Problems :
-----------------

1. https://leetcode.com/problems/longest-substring-with-at-least-k-repeating-characters/description/

--------------------------------------------------------------------------------------------

DP vs Greedy :

If future decisions affect current best choice -> Use DP
If a local best choice guarantees global best -> Use Greedy

--------------------------------------------------------------------------------------------

For DSA problems, we usually analyze performance using time and space complexity.
In production systems we might use benchmarking tools like JMH (Java Microbenchmark Harness),
but for interviews and problem-solving, asymptotic analysis and input scaling are sufficient

--------------------------------------------------------------------------------------------

Number Theory :

This number theory is much more oriented towards Prime numbers, GCDs, LCMs.
Prime Numbers Note: Do study about O(sqrt(n)) and O(log(n)) factorization of some number n.
Theory: 1.  Sieve of Eratosthenes

--------------------------------------------------------------------------------------------

Introduction to Two Pointers : (Pattern)
------------------------------

- Liner data structure or can be traversed in a linear fashion
- Process pairs: Process data elements at two different positions simultaneously.
- Dynamic pointer movement: Both pointers move independently of each other according to certain conditions or criteria.

PS : In addition, both pointers might move along the same or two different data structures.
Examples : valid palindrome, reversing an array, find pair with given sum, etc

--------------------------------------------------------------------------------------------

Fast and Slow Pointer : (Pattern)
-----------------------

- Liner data structure or can be traversed in a linear fashion
- The problem involves detecting a loop or finding an intersection between two linked lists or arrays.
- Find the starting element of the second quantile

--------------------------------------------------------------------------------------------

Sliding Window : (Pattern)
----------------

- The sliding window pattern is a technique for efficiently processing sequential data.
- variation of the two-pointer approach, where the pointers define the window’s start and end

the sliding window can be of a fixed size or dynamic :

1. fixed-size window is used when the window size is given or constant
2. dynamic window is used when the window size changes based on conditions

--------------------------------------------------------------------------------------------

Intervals : (Pattern)
-----------

This pattern is widely used in scheduling, managing resources, and timelines.
By analyzing how intervals interact, we can:

- Merge overlapping intervals into one.
- Insert a new interval into the correct place.
- Find intersections between intervals.
- Detect gaps to identify free time.
- Measure coverage or resource usage.

When working with multiple intervals, they can relate to each other in a few key ways:

- Nonoverlapping    [1,3] and [5,8]
- Overlapping       [1,4] and [3,6]
- Contained         [1,8] and [2,5]

--------------------------------------------------------------------------------------------

Comparable vs Comparator (Core Idea) :
--------------------------------------

🔹 Comparable → Natural ordering
	•	Implemented by the class itself
	•	Method: compareTo()
	•	Used when objects have one obvious sorting logic

class Student implements Comparable<Student> {
    int marks;
    public int compareTo(Student other) {
        return this.marks - other.marks;   // ascending
    }
}

🔹 Comparator → Custom / multiple orderings
	•	Separate class or lambda
	•	Method: compare()
	•	Used when:
	    •	Multiple sorting criteria
	    •	You cannot modify the class (e.g., library classes)

Comparator<Student> byMarksDesc = (a, b) -> b.marks - a.marks;

📌 How comparison works internally : (for both)

Return value           Meaning
< 0                 a comes before b
  0                 equal
> 0                 a comes after b


NOTE : Prefer Comparator over Comparable unless the ordering is fundamental to the object.
--------------------------------------------------------------------------------------------

Longest common subsequence algorithm used in Foundational model evaluation in GenAI

KNN - K nearest neighbour algo used in Amazon OpenSearch service for RAG vector database

Create a web crawler - LLD (used on RAG knowledge base in Amazon bedrock